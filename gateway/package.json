{
  "name": "rewrite-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Auth + rate-limited LLM proxy for the Rewrite app (free models behind newsletter signup).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.6.3"
  },
  "devDependencies": {
    "wrangler": "^3.80.0",
    "typescript": "^5.5.0",
    "@cloudflare/workers-types": "^4.20240909.0"
  }
}
